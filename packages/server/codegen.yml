overwrite: true
schema: "src/schema.gql"
documents: null
config:
  scalars:
    DateTime: Date
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
