overwrite: true
schema: "./schema/**.gql"
documents: null
config:
  scalars:
    ID: string
    DateTime: Date
    JSON: any
  useIndexSignature: true
  #defaultMapper: ./types#DeepPartial<{T}>
generates:
  src/graphql.schema.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
